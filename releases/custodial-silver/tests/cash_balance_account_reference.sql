-- Cash Balance -> Account: (CUSTODIAN_ID, ACCOUNT_NUMBER) must exist in ACCOUNT. Returns rows with no match.
SELECT e."CUSTODIAN_ID", e."ACCOUNT_NUMBER", e."CURRENCY", e."BALANCE_TYPE", e."AS_OF_DATE"
FROM {{ DATABASE }}."SILVER"."CASH_BALANCE" AS e
LEFT JOIN {{ DATABASE }}."SILVER"."ACCOUNT" AS r ON r."CUSTODIAN_ID" = e."CUSTODIAN_ID" AND r."ACCOUNT_NUMBER" = e."ACCOUNT_NUMBER"
WHERE r."CUSTODIAN_ID" IS NULL;
