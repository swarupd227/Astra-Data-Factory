{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://astra.artizent.com/schemas/bundle-v0.schema.json",
  "title": "Release bundle manifest, version 0",
  "description": "Describes how a release bundle (product spec Appendix A) is deployed and tested. Steps run in order; each is a SQL file relative to the bundle directory. Tests are SQL queries that return failing rows; zero rows is a pass.",
  "type": "object",
  "additionalProperties": false,
  "required": ["bundle", "version", "source", "steps"],
  "properties": {
    "bundle": { "type": "string", "pattern": "^[a-z][a-z0-9-]{0,62}$" },
    "version": { "type": "string", "minLength": 1 },
    "source": { "type": "string", "pattern": "^[a-z][a-z0-9_]{0,62}$", "description": "Source id of the config this bundle was rendered from." },
    "steps": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/relative_path" }
    },
    "tests": {
      "type": "array",
      "items": { "$ref": "#/$defs/relative_path" },
      "description": "Files or globs relative to the bundle directory."
    }
  },
  "$defs": {
    "relative_path": {
      "type": "string",
      "pattern": "^(?!/)(?!.*\\.\\.)[A-Za-z0-9_./*-]+\\.sql$"
    }
  }
}
